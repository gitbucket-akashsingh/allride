name: Test Workflow
on: [push, workflow_dispatch] # workflow_dispatch allows manual triggering

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - run: echo "Hello World"
